# ============================================
# GB Guide — Web (Next.js) Dockerfile
# ============================================
FROM node:20-alpine

WORKDIR /app

# Install dependencies first (layer caching)
COPY package.json package-lock.json* ./
RUN npm install

# Copy application code
COPY . .

EXPOSE 3000

# Run Next.js dev server
CMD ["npm", "run", "dev"]
